using System;

namespace Statements
{
    class Program
    {
        static void Main(string[] args)
        {
        //    double funNum = 3;
        //    double numOfStudents = 8;
        //    string stringNum = "7";
        //    int randomNum = 1532;
        //    double anotherRandomNum = 54;
        //    double totalAtmDouble = randomNum / anotherRandomNum;
        //    decimal randomNumDec = 1532;
        //    decimal anotherRandomNumDec = 54;
        //    decimal totalAtmDec = randomNumDec / anotherRandomNumDec;
        //    int totalAtmInt = 1532 / 54;
           

        //    Console.WriteLine(funNum + stringNum + numOfStudents);
        //    Console.WriteLine(funNum + numOfStudents + stringNum);
        //    Console.WriteLine("double value: " + totalAtmDouble + "\ndecimal value: " + totalAtmDec);
        //    Console.WriteLine($"int value: {totalAtmInt}");

        // int totalMinutes = 87;
        // int hours =  (int)(totalMinutes / 60.0 );
        // int minutes = totalMinutes % 60;
        // Console.WriteLine(totalMinutes + " is equal to " + hours + "hour and " + minutes + " minutes");

        // const int DAYS_IN_WEEK = 7;
        // DAYS_IN_WEEK = 8;

        // Console.WriteLine("What is your first name?: ");
        // Console.ForegroundColor = ConsoleColor.Cyan;
        // string firstName = Console.ReadLine();
        // Console.ForegroundColor = ConsoleColor.White;

        // Console.Write("What is your last name?: ");
        // Console.ForegroundColor = ConsoleColor.Cyan;
        // string lastName = Console.ReadLine();
        // Console.ForegroundColor = ConsoleColor.White;

        // Console.Write("How old are you?: ");
        // int age = int.Parse(Console.ReadLine());

        // Console.WriteLine("Hello " +  firstName + " " + lastName + " and you're " + age + " years old!");

        // Console.Write("Give me a number: ");
        // string data = Console.ReadLine();
        int number = int.Parse("2.5");
        Console.WriteLine(number);



        }
    }
}
